package de.fhdw.ergoholics.brainphaser.model;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import de.fhdw.ergoholics.brainphaser.model.Completed;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COMPLETED".
*/
public class CompletedDao extends AbstractDao<Completed, Long> {

    public static final String TABLENAME = "COMPLETED";

    /**
     * Properties of entity Completed.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Class = new Property(1, Integer.class, "class", false, "CLASS");
        public final static Property TimeLastCompleted = new Property(2, java.util.Date.class, "timeLastCompleted", false, "TIME_LAST_COMPLETED");
        public final static Property UserId = new Property(3, long.class, "userId", false, "USER_ID");
        public final static Property ChallengeId = new Property(4, long.class, "challengeId", false, "CHALLENGE_ID");
    };

    private DaoSession daoSession;

    private Query<Completed> user_UserCompletionsQuery;

    public CompletedDao(DaoConfig config) {
        super(config);
    }
    
    public CompletedDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COMPLETED\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CLASS\" INTEGER," + // 1: class
                "\"TIME_LAST_COMPLETED\" INTEGER," + // 2: timeLastCompleted
                "\"USER_ID\" INTEGER NOT NULL ," + // 3: userId
                "\"CHALLENGE_ID\" INTEGER NOT NULL );"); // 4: challengeId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COMPLETED\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Completed entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer class = entity.getClass();
        if (class != null) {
            stmt.bindLong(2, class);
        }
 
        java.util.Date timeLastCompleted = entity.getTimeLastCompleted();
        if (timeLastCompleted != null) {
            stmt.bindLong(3, timeLastCompleted.getTime());
        }
        stmt.bindLong(4, entity.getUserId());
        stmt.bindLong(5, entity.getChallengeId());
    }

    @Override
    protected void attachEntity(Completed entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Completed readEntity(Cursor cursor, int offset) {
        Completed entity = new Completed( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // class
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // timeLastCompleted
            cursor.getLong(offset + 3), // userId
            cursor.getLong(offset + 4) // challengeId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Completed entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setClass(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setTimeLastCompleted(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setUserId(cursor.getLong(offset + 3));
        entity.setChallengeId(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Completed entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Completed entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "userCompletions" to-many relationship of User. */
    public List<Completed> _queryUser_UserCompletions(long userId) {
        synchronized (this) {
            if (user_UserCompletionsQuery == null) {
                QueryBuilder<Completed> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.UserId.eq(null));
                user_UserCompletionsQuery = queryBuilder.build();
            }
        }
        Query<Completed> query = user_UserCompletionsQuery.forCurrentThread();
        query.setParameter(0, userId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getChallengeDao().getAllColumns());
            builder.append(" FROM COMPLETED T");
            builder.append(" LEFT JOIN CHALLENGE T0 ON T.\"CHALLENGE_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Completed loadCurrentDeep(Cursor cursor, boolean lock) {
        Completed entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Challenge challengeCompletions = loadCurrentOther(daoSession.getChallengeDao(), cursor, offset);
         if(challengeCompletions != null) {
            entity.setChallengeCompletions(challengeCompletions);
        }

        return entity;    
    }

    public Completed loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Completed> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Completed> list = new ArrayList<Completed>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Completed> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Completed> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
